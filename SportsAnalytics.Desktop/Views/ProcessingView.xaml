<UserControl x:Class="SportsAnalytics.Desktop.Views.ProcessingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SportsAnalytics.Desktop.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:ProcessingViewModel}"
             Background="#1E1E1E">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Glassmorphism Card -->
            <Border Background="#252526" BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="12" Padding="40" Width="500">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Opacity="0.3" ShadowDepth="0" Color="#007ACC"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="جاري تحليل البيانات واستخراج التوقعات" FontSize="22" FontWeight="Bold" Foreground="White" TextAlignment="Center" Margin="0,0,0,20"/>
                    
                    <ProgressBar IsIndeterminate="{Binding IsIndeterminate}" Value="{Binding ProgressValue}" Minimum="0" Maximum="100" 
                                 Height="8" Background="#333337" Foreground="#007ACC" BorderThickness="0" Margin="0,0,0,30"/>
                    
                    <TextBlock Text="{Binding StatusMessage}" FontSize="16" Foreground="#AAA" TextAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
