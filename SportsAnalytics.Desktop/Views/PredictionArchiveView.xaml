<UserControl x:Class="SportsAnalytics.Desktop.Views.PredictionArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:SportsAnalytics.Desktop.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodels:PredictionArchiveViewModel}"
             Background="#1E1E1E">
    <Grid Margin="20" FlowDirection="RightToLeft">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="← رجوع" Command="{Binding GoBackCommand}" Padding="15,8" Margin="0,0,15,0" 
                    Background="#333337" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
            <TextBlock Text="أرشيف التوقعات" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Archives}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True"
                  Background="#252526" Foreground="White" RowBackground="#1E1E1E" AlternatingRowBackground="#2D2D30"
                  BorderBrush="#3F3F46" HorizontalGridLinesBrush="#3F3F46" VerticalGridLinesBrush="Transparent"
                  HeadersVisibility="Column" SelectionMode="Single" RowHeight="35">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#3F3F46"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#007ACC"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="تاريخ المباراة" Binding="{Binding MatchDate, StringFormat=yyyy-MM-dd HH:mm}" Width="150"/>
                <DataGridTextColumn Header="المباراة" Binding="{Binding MatchName}" Width="200"/>
                
                <DataGridTextColumn Header="احتمال المستضيف" Binding="{Binding PredictedHomeWin, StringFormat=P2}" Width="120"/>
                <DataGridTextColumn Header="احتمال التعادل" Binding="{Binding PredictedDraw, StringFormat=P2}" Width="120"/>
                <DataGridTextColumn Header="احتمال الضيف" Binding="{Binding PredictedAwayWin, StringFormat=P2}" Width="120"/>
                
                <DataGridTextColumn Header="النتيجة الفعلية" Binding="{Binding ActualResult}" Width="100"/>
                
                <DataGridTemplateColumn Header="صحة التوقع" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IsCorrect}" 
                                       FontWeight="Bold" 
                                       HorizontalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCorrect}" Value="{x:Null}">
                                                <Setter Property="Text" Value="قيد الانتظار" />
                                                <Setter Property="Foreground" Value="Gray" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsCorrect}" Value="True">
                                                <Setter Property="Text" Value="نجاح" />
                                                <Setter Property="Foreground" Value="#4CAF50" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsCorrect}" Value="False">
                                                <Setter Property="Text" Value="فشل" />
                                                <Setter Property="Foreground" Value="#F44336" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="2" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="جاري التحميل..." Foreground="White" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
